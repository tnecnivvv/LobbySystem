# Player Data Configuration
playerData:
  networklevel: 0                      # Player's network level (default: 0)
  networkrank: "Player"                # Player's network rank (default: "Player")
  sushi: 1000                           # Player's sushi balance (default: 1000)
  termsofservice: false                 # Player's acceptance of terms of service (default: false)
  language: "en_US"                    # Player's language preference (default: "en_US")